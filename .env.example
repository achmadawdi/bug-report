# Vercel Blob (connect a store under Storage → Blob → Connect Store).
# OIDC (recommended on Vercel): BLOB_STORE_ID + VERCEL_OIDC_TOKEN (injected at runtime).
BLOB_STORE_ID=
# Legacy/manual auth (local scripts or non-Vercel hosts):
BLOB_READ_WRITE_TOKEN=
