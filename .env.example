# Neon PostgreSQL
# Use separate databases (or Neon branches) for local, Vercel Preview, and Production.
# Never point preview deployments at the production DATABASE_URL.
DATABASE_URL=

# Cloudflare R2 (S3-compatible)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_BASE_URL=
