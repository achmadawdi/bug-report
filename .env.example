# Required on Vercel for persistent report and evidence storage.
# Connect a Blob store to the Vercel project (Storage → Blob → Connect Store);
# Vercel injects this token automatically — then redeploy.
BLOB_READ_WRITE_TOKEN=
